# 开火短剧开放平台 — 产品需求文档

---

## 一、项目概述

**平台名称**：开火短剧开放平台

**定位**：面向短剧合作方的开放平台，合作方可在此上传短剧内容，平台方对内容进行审核评级，并提供素材管理和成片上传等能力。

**目标用户**：
- 提报方（合作方）：上传短剧、查看审核状态、上传成片和素材
- 审核方（平台方）：审核短剧内容、给出评级

---

## 二、页面结构

平台包含以下核心页面：

| 页面 | 路径 | 说明 |
|------|------|------|
| 登录页 | `/login` | 用户登录入口 |
| 我的提报 | `/submitter` | 提报方主页，查看剧集列表和上传新剧（图1） |
| 上传新剧 | `/submitter/upload` | 填写新剧信息表单（图2） |
| 剧集详情 | `/submitter/drama/:id` | 查看剧集详情、交付要求、素材管理（图3） |
| 成片上传 | `/submitter/drama/:id/episodes` | 上传分集视频成片（图4） |
| 审核后台 | `/auditor` | 审核方后台，查看待审核列表、执行审核 |

---

## 三、全局布局与样式

### 3.1 顶部导航栏
- **高度**：56px
- **背景色**：深色渐变（#1a1a2e → #16213e）
- **左侧**：平台Logo图标 + 平台名称「开火短剧开放平台」
- **右侧**：用户角色标签（如「提报人」）+ 用户名 + 「退出」按钮
- **固定定位**：页面滚动时始终置顶

### 3.2 页面主体
- **背景色**：#f5f6fa（浅灰白）
- **内容区最大宽度**：1200px，水平居中
- **内边距**：padding 24px

### 3.3 配色方案
| 用途 | 色值 |
|------|------|
| 主色调（品牌色）| #e83e8c（玫红/粉红，用于按钮、链接、高亮） |
| 成功/通过 | #52c41a（绿色） |
| 警告/待审核 | #faad14（黄色/橙色） |
| 错误/不通过 | #ff4d4f（红色） |
| 深色导航 | #1a1a2e |
| 页面背景 | #f5f6fa |
| 卡片背景 | #ffffff |
| 文字主色 | #1f1f1f |
| 文字次要 | #8c8c8c |
| 边框 | #d9d9d9 |

---

## 四、页面详细需求

---

### 4.1 登录页 `/login`

**布局**：居中登录卡片

**元素**：
- 平台Logo + 名称
- 用户名输入框
- 密码输入框
- 角色选择：提报人 / 审核人（单选）
- 登录按钮
- 登录后根据角色跳转：提报人→我的提报，审核人→审核后台

---

### 4.2 我的提报页 `/submitter`（对应图1）

#### 4.2.1 页面头部
- **标题**：「我的提报」，字号 24px，加粗
- **副标题**：「管理您上传的剧集内容」，字号 14px，颜色 #8c8c8c

#### 4.2.2 统计卡片区
横向排列 4 个统计卡片，每个卡片：
- 背景白色，圆角 12px，阴影 0 2px 8px rgba(0,0,0,0.08)
- 左侧图标 + 右侧数字和标签

| 图标 | 数字 | 标签 |
|------|------|------|
| ALL 图标（粉色背景圆） | 动态数字 | 我的剧集 |
| 横线图标（黄色背景圆） | 动态数字 | 待审核 |
| 省略号图标（橙色背景圆） | 动态数字 | 审核中 |
| OK 图标（绿色背景圆） | 动态数字 | 已完成 |

- 点击卡片可筛选下方列表对应状态

#### 4.2.3 Tab 切换
- 「剧集列表」和「+ 上传新剧」两个 Tab
- 点击「+ 上传新剧」切换到上传表单视图（对应图2）

#### 4.2.4 搜索与筛选区
- 搜索输入框：placeholder「搜索剧名...」，宽度 280px
- 时效性下拉筛选：选项包含「全部时效」「T-7」「T-15」「T-30」等
- 搜索按钮：粉色背景，白色文字「搜索」

#### 4.2.5 剧集列表表格
表格列定义：

| 列名 | 宽度 | 说明 |
|------|------|------|
| 剧名 | flex | 左对齐，玫红色文字，可点击跳转详情 |
| 类型 | 80px | 「真人」或「AI剧」 |
| 时效性 | 80px | 如「T-7」「T-15」 |
| 上传日期 | 160px | 格式 2026-05-18 11:58:07 |
| 状态 | 100px | 见下方状态标签 |
| 审核结果 | 100px | 见下方结果标签 |
| 操作 | 200px | 操作按钮组 |

**状态标签样式**：
- 待审核：蓝色背景圆点 + 蓝色文字
- 审核中：橙色背景圆点 + 橙色文字
- 已完成：绿色背景圆点 + 绿色文字

**审核结果标签样式**：
- S级：金色/黄色背景标签
- A+级：绿色背景标签
- A级：浅绿背景标签
- B级：浅黄背景标签
- 不通过：红色背景标签
- 未审核：显示「—」

**操作按钮**：
- 「详情」：跳转剧集详情页
- 「素材花絮」：跳转素材管理（锚点定位到素材区）
- 「上传成片」：跳转成片上传页
- 按钮样式：白色背景，粉色边框，粉色文字，圆角 6px，hover 时粉色背景白色文字

**表格交互**：
- 点击剧名跳转详情页
- 表格行 hover 时背景色变为 #fafafa
- 空状态显示「暂无剧集数据」

---

### 4.3 上传新剧表单（集成在我的提报页 Tab 中）（对应图2）

点击「+ 上传新剧」Tab 后，在原列表位置替换为表单：

**表单标题**：「上传新剧」，字号 18px，加粗

**表单字段**：

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| 剧名 | 文本输入框 | 是 | placeholder「请输入剧名」 |
| 视频地址 | 文本输入框 | 是 | placeholder「请输入视频地址或者网盘地址」 |
| 类型 | 单选按钮组 | 是 | 选项：真人 / AI剧 |
| 版权类型 | 单选按钮组 | 是 | 选项：定制剧 / 成品剧 |
| 花絮卡点描述 | 多行文本框 | 否 | placeholder「例如：卡1：第11集，卡2：第25集」 |
| 花絮、卡点 | 文件上传区 | 否 | 支持拖拽上传视频/图片，支持多文件 |

**文件上传区样式**：
- 虚线边框（#d9d9d9），圆角 8px
- 中心显示「+」大号图标（粉色）
- 下方文字：「点击或拖拽视频/图片文件到此处」
- 小字提示：「支持多文件同时上传」
- 拖拽进入时边框变为粉色实线

**提交区域**：
- 「提交上传」按钮：粉色渐变背景（#e83e8c → #ff6b9d），白色文字，圆角 8px
- 「取消」按钮：白色背景，灰色边框，灰色文字

**表单校验**：
- 必填项为空时提示「请输入XXX」
- 提交成功后提示「上传成功」，自动切换到剧集列表 Tab 并刷新数据

---

### 4.4 剧集详情页 `/submitter/drama/:id`（对应图3）

**页面布局**：左右分栏
- 左侧：交付需求说明面板，宽度 380px，固定
- 右侧：素材列表区域，自适应剩余宽度

#### 4.4.1 左侧 — 交付需求说明面板

**标题栏**：「交付需求说明」+ 右上角评级标签（如「A+ 至 S+」粉色标签）

**（1）特殊要求区域**
- 标题：「特殊要求（A+ 至 S+ 评级）」带黄色星星图标
- 内容：
  > 需额外交付一个网盘链接，包含：
  > - 30%（10-15集）带字幕分集成片
  > - 5条高光预告片
  > - 1:1海报

**（2）交付版本表格**
- 标题：「交付版本」带文件图标
- 两列表格：类型 | 说明

| 类型 | 说明 |
|------|------|
| 完整版MP4 | 有字幕,有背景音乐,无片头片尾整集 |
| 完整版本SRT文件 | 无字幕版本 |
| 分集版本MP4 | 有字幕,有背景音乐,分集版本 |
| 分集版本SRT文件 | 分集版本字幕文件 |

**（3）物料要求表格**
- 标题：「物料要求」带圆形图标
- 两列表格：物料 | 内容

| 物料 | 内容 |
|------|------|
| 简介 | 长版简介200-300字 + 长版简介500-1000字（Word文档） |
| 制作信息 | 剧名,题材,频道,导演（真名+笔名）,导演性别,制片人（真名+笔名）,制片人性别,编剧（真名+笔名）,编剧性别,角色姓名(女主/男主/女配/男配),角色介绍20字以上,剧集集数/总时间,平均单集时长,剧集制作成本,制作机构,剧集推荐文案(高光剧情描述),推荐首次付费集数,推荐语（12字以内） |
| 宣传图/封面图 | 封面：尺寸9:16和7:10和2:3，大小不超过5M；女主/男主/女配/男配——角色照片，大小限制在200K-5M |

**（4）真人/原创额外素材**
- 标题带电影图标
- 两列表格：素材 | 说明

| 素材 | 说明 |
|------|------|
| 宣传素材 | 尽量psd版本。剧情回顾:关键剧情创意剪辑，有主题延展和剧情重编的视频作品(剧情主题/事件主题/场景主题/情绪主题等)。剧情解析：关键剧情深度解析,有深度延展和剧情解读的视频内容(如剧情解读人物解读) |
| 工程文件（4张）或现场拍摄图（1张）二选一 | 现场拍摄图：1张带有剧组/剧名横幅的开机截图，若无横幅提供其他真实拍摄图均可。工程文件：4张带有制作分轨、剪辑点的工程文件截图 |

#### 4.4.2 右侧 — 素材列表

**标题**：「素材列表（0）」括号内显示素材数量

**空状态**：
- 居中显示「暂无素材」
- 「+ 上传素材」按钮：粉色边框，点击打开素材上传弹窗

**有数据时**：
- 网格布局展示素材卡片
- 每个卡片：缩略图 + 素材名称 + 上传时间 + 操作按钮（下载、删除）

**素材上传弹窗**：
- 标题：「上传素材」
- 拖拽上传区域
- 支持文件类型：图片、视频、文档
- 确认/取消按钮

---

### 4.5 成片上传页 `/submitter/drama/:id/episodes`（对应图4）

**页面标题**：「成片上传」，字号 24px，加粗

#### 4.5.1 上传新剧集区域
- 卡片容器：白色背景，圆角 12px，边框 1px solid #e8e8e8

**提示文字**：
> 文件请以「顺序号_分集标题」命名（如 1_第一集.mp4），系统自动识别集数

**文件选择器**：
- 标签：「视频文件（可多选）」
- 原生文件选择按钮「选择文件」，右侧显示已选文件名
- 限制：仅接受 .mp4, .mov 格式
- 「开始上传」按钮：蓝色背景（#4a90e2），白色文字，圆角 6px

**上传过程**：
- 显示每个文件的上传进度条
- 上传完成后显示「✓ 上传成功」

#### 4.5.2 已上传剧集区域
- 卡片容器样式同上
- 标题：「已上传剧集」
- 展示已上传的分集列表：

| 列名 | 说明 |
|------|------|
| 集数 | 第X集 |
| 标题 | 分集标题 |
| 文件名 | 原始文件名 |
| 大小 | 文件大小 |
| 上传时间 | 上传完成时间 |
| 操作 | 删除、替换 |

- 空状态：「暂无已上传的剧集」

---

### 4.6 审核后台 `/auditor`

（注：参考图主要展示提报方视角，审核方界面需根据业务流程补充设计）

#### 4.6.1 页面布局
- 顶部导航同全局，角色显示「审核人」
- 左侧菜单：待审核列表、已审核记录、评级标准
- 右侧主内容区

#### 4.6.2 待审核列表
- 表格列：剧名、类型、版权类型、提报人、上传日期、操作
- 「审核」按钮打开审核弹窗

#### 4.6.3 审核弹窗
- 剧集基本信息展示
- 视频预览区（播放器嵌入）
- 审核结果选择：
  - 评级：S级 / A+级 / A级 / B级 / 不通过
  - 审核意见：多行文本框
- 确认/取消按钮

#### 4.6.4 已审核记录
- 可按日期、评级、类型筛选
- 表格展示所有已审核剧集及结果

---

## 五、数据模型

### 5.1 剧集（Drama）

```typescript
interface Drama {
  id: string;              // 唯一ID
  name: string;            // 剧名
  videoUrl: string;        // 视频地址/网盘地址
  type: '真人' | 'AI剧';    // 类型
  copyrightType: '定制剧' | '成品剧'; // 版权类型
  highlightDescription: string; // 花絮卡点描述
  timeliness: string;      // 时效性，如 T-7
  status: '待审核' | '审核中' | '已完成'; // 审核状态
  auditResult: 'S级' | 'A+级' | 'A级' | 'B级' | '不通过' | null; // 审核结果
  submitterId: string;     // 提报人ID
  submitterName: string;   // 提报人姓名
  createdAt: string;       // 上传日期
  updatedAt: string;       // 更新时间
}
```

### 5.2 素材（Material）

```typescript
interface Material {
  id: string;
  dramaId: string;         // 所属剧集ID
  name: string;            // 素材名称
  type: '图片' | '视频' | '文档'; // 素材类型
  url: string;             // 文件地址
  size: number;            // 文件大小（字节）
  createdAt: string;       // 上传时间
}
```

### 5.3 剧集分集（Episode）

```typescript
interface Episode {
  id: string;
  dramaId: string;         // 所属剧集ID
  episodeNumber: number;   // 集数序号
  title: string;           // 分集标题
  fileName: string;        // 原始文件名
  fileSize: number;        // 文件大小
  url: string;             // 视频地址
  createdAt: string;       // 上传时间
}
```

### 5.4 用户（User）

```typescript
interface User {
  id: string;
  username: string;        // 用户名
  displayName: string;     // 显示名称
  role: 'submitter' | 'auditor'; // 角色：提报人/审核人
  avatar?: string;         // 头像URL
}
```

---

## 六、交互说明

### 6.1 路由与导航
- 未登录用户访问任何页面重定向到 `/login`
- 提报人访问审核后台重定向到「我的提报」
- 审核人访问提报页面重定向到「审核后台」

### 6.2 状态流转
```
上传新剧 → 待审核 → 审核中 → 已完成（有评级）
                    ↘ 不通过
```

### 6.3 交互细节
1. **统计卡片点击**：点击后下方列表按对应状态筛选，卡片有 hover 抬升效果
2. **剧名链接**：hover 时显示下划线，点击跳转详情
3. **文件上传**：拖拽进入区域时边框高亮，上传过程显示进度
4. **表格操作按钮**：hover 时反色效果
5. **表单提交**：loading 状态防重复提交
6. **删除操作**：二次确认弹窗

---

## 七、Mock 数据建议

### 7.1 我的提报页 Mock 数据
- 总剧集数：24
- 待审核：1
- 审核中：0
- 已完成：22
- 列表展示 5-10 条示例数据，包含各种状态和评级

### 7.2 示例剧集数据
```json
{
  "name": "谁抢机缘谁赢麻，我竟是隐藏BOSS",
  "type": "AI剧",
  "timeliness": "T-7",
  "status": "已完成",
  "auditResult": "S级"
}
```

---

## 八、技术建议

- **前端框架**：React + TypeScript + Vite
- **UI 组件库**：Ant Design 5.x
- **路由**：React Router v6
- **状态管理**：React Hooks / Zustand
- **样式方案**：CSS Modules 或 styled-components

---

## 九、附录：参考图功能对照

| 参考图 | 对应页面 | 核心功能 |
|--------|----------|----------|
| 图1 | 我的提报 `/submitter` | 统计卡片、搜索筛选、剧集列表表格 |
| 图2 | 上传新剧（Tab内） | 表单填写、文件拖拽上传 |
| 图3 | 剧集详情 `/submitter/drama/:id` | 交付需求说明、素材管理 |
| 图4 | 成片上传 `/submitter/drama/:id/episodes` | 分集视频上传、已传列表 |
