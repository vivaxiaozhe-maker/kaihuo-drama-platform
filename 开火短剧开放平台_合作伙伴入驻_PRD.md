# 开火短剧开放平台 — 合作伙伴入驻提报功能需求文档

---

## 一、需求概述

在现有「开火短剧开放平台」基础上，新增**合作伙伴入驻申请**流程。新合作方（短剧制作方、版权方、MCN机构等）可通过登录页的「入驻平台」入口，在线提交入驻申请，经平台审核通过后成为正式提报方。

**参考来源**：小米内容开放平台的「企业信息管理」「内容上传」「协议列表」「内容版权管理」等模块（见参考截图）。

---

## 二、现有登录页修改

### 2.1 登录表单下方新增区域

在现有登录表单（用户名/密码/角色选择/登录按钮）下方，增加一行：

```
还没有账号？  入驻平台（注册）
```

**样式**：
- 文字：「还没有账号？」颜色 #8c8c8c，字号 14px
- 「入驻平台」按钮：粉色文字（#e83e8c），无背景，hover 时显示下划线，字号 14px
- 「注册」文字用括号标注，表示该流程含注册功能
- 整体居中于登录卡片底部，与登录按钮间距 20px

**点击行为**：点击「入驻平台」跳转入驻申请流程首页 `/onboarding`

---

## 三、入驻申请流程（四步步骤条）

**页面路径**：`/onboarding`
**布局**：顶部步骤条 + 底部表单区（白色卡片）

```
步骤1: 企业信息  →  步骤2: 联系人设置  →  步骤3: 业务能力  →  步骤4: 协议与提交
```

**步骤条样式**：
- 横向居中排列，圆圈内数字 + 步骤名称
- 当前步骤：粉色圆圈内数字 + 粉色文字
- 已完成步骤：粉色勾选图标 + 粉色文字
- 未完成步骤：灰色空心圆圈 + 灰色文字
- 步骤间用灰色连线连接，已完成的连线为粉色

**表单卡片样式**：
- 宽度 720px，水平居中
- 白色背景，圆角 12px，阴影 0 2px 12px rgba(0,0,0,0.08)
- 内部 padding 40px

**底部操作区**：
- 位于表单卡片内底部，flex 布局，右对齐
- 「上一步」按钮：白色背景，灰色边框，灰色文字（第一步不显示）
- 「下一步」按钮：粉色渐变背景，白色文字
- 最后一步显示「提交审核」按钮（样式同下一步，文字不同）

---

## 四、各步骤字段详单

---

### 步骤1：企业信息

**步骤标题**：「企业信息」，副标题「请填写您的企业/机构基本信息」

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| 企业名称 | 文本输入框 | 是 | placeholder「请输入营业执照上的企业全称」，限50字 |
| 统一社会信用代码 | 文本输入框 | 是 | placeholder「请输入18位统一社会信用代码」，正则校验 |
| 营业执照 | 图片上传 | 是 | 单张，支持 jpg/png/pdf，最大 10MB，需支持预览 |
| 企业类型 | 单选按钮组 | 是 | 文化传媒公司 / 影视制作公司 / MCN机构 / 个人工作室 / 其他（选其他显示输入框） |
| 成立时间 | 日期选择器 | 否 | 格式 YYYY-MM-DD |
| 注册地址 | 级联选择+详细地址 | 否 | 省/市/区三级级联 + 详细地址输入框 |
| 办公地址 | 文本输入框 | 否 | 如与注册地址不同可填写 |

**营业执照上传区样式**：
- 虚线边框（#d9d9d9），圆角 8px，高度 160px
- 中心区域：上传图标 + 「点击上传营业执照」+ 「支持 jpg、png、pdf，最大10MB」
- 上传后：显示缩略图 + 文件名 + 删除按钮

**表单校验**：
- 统一社会信用代码：正则 `^[0-9A-HJ-NPQRTUWXY]{2}\d{6}[0-9A-HJ-NPQRTUWXY]{10}$`
- 企业名称：不能为空，不超过50字

---

### 步骤2：联系人设置

**步骤标题**：「联系人设置」，副标题「请填写企业对接人和法定代表人信息」

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| **对接人信息** | | | |
| 对接人姓名 | 文本输入框 | 是 | 平台日常沟通联系人 |
| 对接人手机号 | 文本输入框 | 是 | 正则校验手机号格式 |
| 对接人邮箱 | 文本输入框 | 是 | 邮箱格式校验 |
| 对接人职务 | 文本输入框 | 否 | 如：商务总监 |
| **法定代表人信息** | | | |
| 法人姓名 | 文本输入框 | 是 | |
| 法人手机号 | 文本输入框 | 是 | |
| 法人身份证号 | 文本输入框 | 是 | 脱敏显示，18位校验 |
| 法人邮箱 | 文本输入框 | 否 | |

**布局说明**：
- 对接人信息和法定代表人信息分为两个区块，用分组标题隔开
- 分组标题样式：左侧粉色竖线（4px宽）+ 标题文字，字号 16px 加粗
- 可选「经办人与对接人相同」复选框，勾选后自动填充（如有经办人字段）

---

### 步骤3：业务能力

**步骤标题**：「业务能力」，副标题「请填写您的内容生产能力和业务资质」

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| 合作业务类型 | 多选框组 | 是 | 成品短剧 / 定制短剧 / AI短剧 / 动漫短剧 |
| 内容类型偏好 | 多选框组 | 否 | 真人实拍 / AI生成 / 动画 / 混合 |
| 男女频偏好 | 单选按钮组 | 否 | 男频 / 女频 / 不限 |
| 擅长题材 | 多选框组 | 否 | 逆袭 / 喜剧 / 异能 / 修仙 / 都市 / 古装 / 甜宠 / 悬疑 / 其他 |
| **资质证书** | | | |
| 广播电视节目制作经营许可证 | 单选 | 是 | 有 / 无 |
| 许可证编号 | 文本输入框 | 条件必填 | 选择「有」时必填 |
| 许可证有效期 | 日期选择器 | 条件必填 | 选择「有」时必填 |
| 许可证扫描件 | 图片上传 | 条件必填 | 选择「有」时必填，单张，最大10MB |
| 网络文化经营许可证 | 单选 | 否 | 有 / 无 |
| 其他资质 | 多行文本框 | 否 | placeholder「如有其他相关资质请说明」 |
| **过往作品** | | | |
| 是否有已上线作品 | 单选按钮组 | 否 | 是 / 否 |
| 作品清单 | 动态表格 | 条件必填 | 选择「是」时显示，可添加多行 |

**过往作品清单表格列**：

| 列名 | 类型 | 说明 |
|------|------|------|
| 作品名称 | 文本输入 | 短剧名称 |
| 上线平台 | 文本输入 | 如：抖音、快手、微信小程序 |
| 集数 | 数字输入 | |
| 类型 | 下拉选择 | 真人 / AI剧 / 动漫 |
| 播放链接 | 文本输入 | 可选填 |
| 操作 | 删除按钮 | 移除该行 |

**表格交互**：
- 默认显示一行空输入
- 底部「+ 添加作品」按钮，点击新增一行
- 最多添加 10 行

---

### 步骤4：协议与提交

**步骤标题**：「协议与提交」，副标题「确认信息并签署合作协议」

#### 4.1 信息确认区
- 以只读卡片形式汇总展示步骤1-3填写的关键信息
- 每项信息旁有「修改」链接，点击跳回对应步骤
- 信息卡片分组：企业信息 / 联系人 / 业务能力

#### 4.2 合作协议区
- 协议标题：「开火短剧开放平台合作协议」
- 协议内容在滚动文本框中展示（placeholder 内容，见下方）
- 底部复选框：「我已阅读并同意《开火短剧开放平台合作协议》」

**协议核心条款（需展示）**：
```
1. 合作方保证所提交内容拥有合法版权或已获得版权方授权。
2. 合作方保证提交内容不违反国家法律法规，不含有色情、暴力、政治敏感等内容。
3. 平台有权对提交内容进行审核，审核结果以平台评定为准。
4. 合作方需按照平台要求提供完整的内容素材和物料。
5. 收益结算以双方另行签署的结算协议为准。
6. 合作方不得将平台提供的内部信息泄露给第三方。
```

#### 4.3 提交按钮
- 「提交审核」按钮：粉色渐变背景（#e83e8c → #ff6b9d），白色文字，圆角 8px
- 未勾选协议时按钮置灰 disabled
- 提交后跳转成功页

---

## 五、入驻成功页

**路径**：`/onboarding/success`
**布局**：居中内容

**元素**：
- 大号成功图标（绿色勾选圆圈，64px）
- 标题：「入驻申请已提交」，字号 24px 加粗
- 副标题：「我们将在 1-3 个工作日内完成审核，审核结果将通过短信和邮件通知您」
- 申请编号：显示本次入驻申请的编号（如 `KH20260519001`）
- 两个按钮：
  - 「返回登录页」→ 跳转 `/login`
  - 「查看申请进度」（可点，预留后续功能）

---

## 六、审核方后台 - 入驻申请管理（新增模块）

在现有审核人后台（`/auditor`）左侧菜单新增「入驻管理」模块。

### 6.1 入驻申请列表 `/auditor/onboarding`

**筛选区**：
- 申请编号：文本输入
- 企业名称：文本输入
- 审核状态：下拉（全部 / 待审核 / 审核中 / 已通过 / 已拒绝）
- 申请时间：日期范围选择
- 操作按钮：重置 / 查询

**表格列**：

| 列名 | 说明 |
|------|------|
| 申请编号 | 如 KH20260519001 |
| 企业名称 | |
| 统一社会信用代码 | 脱敏显示 |
| 合作业务类型 | 成品短剧/定制短剧 等标签 |
| 对接人 | 姓名 + 手机号 |
| 申请时间 | 2026-05-19 14:30:00 |
| 审核状态 | 见下方状态标签 |
| 操作 | 审核 / 查看详情 |

**状态标签样式**：
- 待审核：灰色
- 审核中：蓝色
- 已通过：绿色
- 已拒绝：红色

### 6.2 入驻审核弹窗

点击「审核」按钮弹出：

**弹窗标题**：「入驻审核 - XXX企业」

**内容分 Tab**：
- Tab1「企业信息」：展示步骤1所有信息 + 营业执照预览
- Tab2「联系人」：展示步骤2所有信息
- Tab3「业务能力」：展示步骤3所有信息 + 过往作品列表 + 资质图片预览

**审核操作区**（弹窗底部）：
- 审核结果：通过 / 拒绝（单选）
- 审核意见：多行文本框
- 如选择「通过」，需设置：
  - 合作业务类型确认（多选，默认以申请为准）
  - 生成协议编号
- 如选择「拒绝」：
  - 拒绝原因选择（资质不全/内容不符/重复申请/其他）
  - 补充说明文本框
- 「确认」/「取消」按钮

**审核通过后的系统行为**：
- 自动生成企业账号（用户名：统一社会信用代码，初始密码随机生成）
- 发送短信+邮件通知对接人（含登录账号和密码）
- 状态变为「已通过」

---

## 七、数据模型

### 7.1 入驻申请（OnboardingApplication）

```typescript
interface OnboardingApplication {
  id: string;                    // 申请编号，如 KH20260519001
  // === 步骤1: 企业信息 ===
  companyName: string;           // 企业名称
  unifiedSocialCode: string;     // 统一社会信用代码
  businessLicenseUrl: string;    // 营业执照文件地址
  companyType: string;           // 企业类型
  establishedAt?: string;        // 成立时间
  registeredAddress?: string;    // 注册地址
  officeAddress?: string;        // 办公地址
  
  // === 步骤2: 联系人 ===
  contactPerson: {
    name: string;
    phone: string;
    email: string;
    position?: string;
  };
  legalRepresentative: {
    name: string;
    phone: string;
    idCard: string;
    email?: string;
  };
  
  // === 步骤3: 业务能力 ===
  businessTypes: string[];       // 合作业务类型
  contentTypePreference?: string[]; // 内容类型偏好
  genderPreference?: string;     // 男女频偏好
  genreSpecialty?: string[];     // 擅长题材
  hasBroadcastLicense: boolean;  // 是否有广播电视节目制作经营许可证
  broadcastLicenseNo?: string;   // 许可证编号
  broadcastLicenseExpireAt?: string; // 有效期
  broadcastLicenseUrl?: string;  // 许可证扫描件
  hasNetCultureLicense: boolean; // 是否有网络文化经营许可证
  otherQualifications?: string;  // 其他资质
  hasPreviousWorks: boolean;     // 是否有已上线作品
  previousWorks?: PreviousWork[]; // 过往作品清单
  
  // === 审核相关 ===
  status: 'pending' | 'reviewing' | 'approved' | 'rejected';
  auditResult?: 'approved' | 'rejected';
  auditOpinion?: string;         // 审核意见
  rejectReason?: string;         // 拒绝原因
  reviewerId?: string;           // 审核人ID
  reviewedAt?: string;           // 审核时间
  
  // === 系统字段 ===
  createdAt: string;             // 申请时间
  updatedAt: string;             // 更新时间
  accountGenerated?: boolean;    // 是否已生成账号
  accountUsername?: string;      // 生成的登录账号
}

interface PreviousWork {
  name: string;                  // 作品名称
  platform: string;              // 上线平台
  episodes: number;              // 集数
  type: string;                  // 类型
  link?: string;                 // 播放链接
}
```

### 7.2 企业信息（Company）- 审核通过后生成

```typescript
interface Company {
  id: string;
  applicationId: string;         // 关联的入驻申请ID
  companyName: string;
  unifiedSocialCode: string;
  companyType: string;
  businessLicenseUrl: string;
  status: 'active' | 'suspended' | 'terminated';
  contactPerson: ContactPerson;
  legalRepresentative: LegalRepresentative;
  businessTypes: string[];
  createdAt: string;
}
```

---

## 八、页面路由汇总

| 路径 | 页面 | 权限 |
|------|------|------|
| `/login` | 登录页（新增入驻入口） | 公开 |
| `/onboarding` | 入驻申请（四步流程） | 公开 |
| `/onboarding/success` | 入驻成功页 | 公开 |
| `/auditor/onboarding` | 入驻申请管理列表 | 审核人 |

---

## 九、交互说明

### 9.1 步骤条导航
- 点击已完成的步骤可回退到该步骤修改
- 当前步骤表单校验通过后才可进入下一步
- 离开页面时如填写了内容，提示「确认离开？已填写的内容将不会保存」

### 9.2 表单自动保存
- 每步表单内容自动保存到 localStorage（key: `onboarding_draft`）
- 用户刷新页面后可恢复填写进度
- 提交成功后清除 localStorage

### 9.3 图片上传
- 点击上传区或拖拽文件到上传区均可触发上传
- 上传中显示进度动画
- 上传失败显示错误提示，可重试
- 支持删除已上传图片并重新上传

### 9.4 提交防重复
- 点击「提交审核」后按钮进入 loading 状态
- 接口响应前禁止重复点击
- 后端对接口做幂等处理（以统一社会信用代码去重）

---

## 十、Mock 数据建议

### 10.1 入驻申请列表演示数据
建议准备 5-8 条不同状态的入驻申请：
- 2条「待审核」
- 1条「审核中」
- 3条「已通过」（含不同企业类型）
- 1条「已拒绝」

### 10.2 示例数据
```json
{
  "id": "KH20260519001",
  "companyName": "北京晓越麦塔文化传媒有限公司",
  "unifiedSocialCode": "91110105MABXW0EJ6B",
  "companyType": "文化传媒公司",
  "businessTypes": ["成品短剧", "AI短剧"],
  "contactPerson": {
    "name": "张小明",
    "phone": "138****5678"
  },
  "status": "approved",
  "createdAt": "2026-05-18 10:23:15"
}
```

---

## 十一、参考截图功能对照表

| 参考图 | 小米平台模块 | 本次需求对应 |
|--------|-------------|-------------|
| 图1 | 内容版权管理（筛选+列表） | 入驻申请列表筛选与表格布局参考 |
| 图2 | 授权文件（文件列表） | 资质证书上传与文件管理参考 |
| 图3 | 内容列表（海报+审核流程） | 入驻列表操作按钮、审核流程弹窗参考 |
| 图4 | 审批记录（状态时间线） | 入驻审核状态流转展示参考 |
| 图5 | 内容上传步骤1（分类/终端） | 四步步骤条UI组件样式参考 |
| 图6 | 内容上传步骤2（媒资信息） | 大量表单字段布局、分组、图片上传区参考 |
| 图7 | 协议列表（Tab+状态标签） | 合作协议管理、状态标签样式参考 |
| 图8 | 企业信息管理（对接人/经办人） | 企业信息表格、操作按钮（对接人/经办人）参考 |
| 图9 | 消息列表（筛选+表格） | 筛选区布局、表格空状态参考 |

---

## 十二、UI 样式要点（与现有平台保持一致）

- **主色调**：#e83e8c（玫红/粉红）
- **成功色**：#52c41a
- **页面背景**：#f5f6fa
- **卡片背景**：#ffffff，圆角 12px
- **文字主色**：#1f1f1f
- **文字次要**：#8c8c8c
- **输入框高度**：40px，圆角 6px，边框 #d9d9d9，focus 时粉色边框
- **按钮高度**：40px
- **步骤条**：与现有平台卡片式风格一致
